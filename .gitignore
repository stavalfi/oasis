node_modules
dist
output

# Secrets — never commit.
.env
.env.*
!.env.example
.claude/scheduled_tasks.lock.direnv/
.direnv
*.tsbuildinfo
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup

# Personal Pulumi stack files (per-dev kind clusters). Shared-stack files
# like Pulumi.dev--main.yaml / Pulumi.prod--main.yaml SHOULD be committed
# so everyone has the same encryptionsalt for stack-level secrets.
Pulumi.*.yaml
devex/generated/**/src
devops/crds/generated/crds2pulumi/src
.serena
frontend/dist

# Generated by kysely-codegen on `bun run backend`; never hand-edited.
backend/src/db/schema.ts
e2e/.chromium-profile
e2e/.auth
test-results
*.log
