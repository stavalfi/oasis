# Jira (Atlassian) OAuth 2.0 (3LO) app credentials.
# Copy this file to `.env` and fill in real values from
# https://developer.atlassian.com/console/myapps/ (Settings -> Authorization).
# `.env` is gitignored and must never be committed.
JIRA_CLIENT_ID=
JIRA_CLIENT_SECRET=
